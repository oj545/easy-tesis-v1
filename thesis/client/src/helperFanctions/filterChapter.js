// import { sections } from '../data/links';

// export const FilterChapters = (chapterName) => {
//   const titelChapter = sections.filter((sec) => sec.name === chapterName);
//   const chapter = titelChapter[0];
//   return chapter;
// };
